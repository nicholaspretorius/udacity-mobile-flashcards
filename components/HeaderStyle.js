const HeaderStyle = title => {
  return {
    title: title,
    headerStyle: {
      backgroundColor: "#000"
    },
    headerTintColor: "#fff",
    headerTitleStyle: {
      fontWeight: "normal"
    }
  };
};

export default HeaderStyle;
